# OSProjectOne
OS Project for learning Multiprocessing

```Driver``` - Takes user Input. Creates subprocess for running Logger and Encryptor.

```Logger``` - Logs information regarding runtime to a text file. Creates a new file if it does not exist.

```Encryptor``` - Encrypts data using the Vignere Cipher encryption-decryption algorithm. User inputs the key for this algorithm.

## Description
